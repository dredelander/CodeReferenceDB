@model IEnumerable<Reference>
@{

    ViewData["Title"] = "Index";
}

<h1>Repo for Code Help References</h1>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Reference List</h2>
        </div>
        <div class="col-6 text-end">
            @*<a asp-controller="Category" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp Create a Category
            </a>*@
        </div>
    </div>

    <br />
    <br />

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Language
                </th>
                <th>
                    Keyphrase
                </th>
                <th>
                    Project Name
                </th>
                <th>
                    Link
                </th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="50%">
                        @obj.Language
                    </td>
                    <td width="50%">
                        @obj.Keyphrase
                    </td>
                    <td width="50%">
                        @obj.ProjectName
                    </td>
                    <td width="50%">
                        @obj.Link
                    </td>
                    @*<td>
            <div class="w-75 btn-group" role="group">
                <a asp-controller="Category" asp-action="Edit" asp-route-id="@obj.Id">Edit</a>
            </div>
        </td>
        <td>
            <div class="w-75 btn-group danger" role="group">
                <a asp-controller="Category" asp-action="Delete" asp-route-id="@obj.Id">Delete</a>
            </div>
        </td>*@

                </tr>
            }
        </tbody>
    </table>
</div>
